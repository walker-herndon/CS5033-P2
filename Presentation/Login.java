package Presentation;

import javax.swing.*;

public class Login extends JFrame {

    public Login() {
        JComponent navigation = new JComponent() {
            // navigation logic
        };

        JComponent login = new JComponent() {
            // login logic, verified by UserManager API call
        };

        setVisible(true);
    }
}
